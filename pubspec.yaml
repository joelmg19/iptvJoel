name: iptv_flutter_app
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Networking
  http: ^1.2.0

  # State management
  flutter_riverpod: ^2.5.1

  # Video / IPTV
  video_player: ^2.8.6
  chewie: ^1.8.1

  # UI helpers
  cached_network_image: ^3.3.1

  # Utilities
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
